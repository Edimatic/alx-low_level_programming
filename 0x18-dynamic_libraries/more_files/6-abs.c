#include "main.h"

/**
 * _abs - checking the code for Holberton School students.
 *
 * @n: is an integer argument
 *
 * Return: Always return 0.
 */

int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}
