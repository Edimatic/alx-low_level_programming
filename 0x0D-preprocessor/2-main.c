#include <stdio.h>

/**
 * main - check the code for Holberton School students.
 * Explanation: a program that prints the name of the file
 * Return: Always return 0 at the end
 */

int main(void)
{
	printf("%s\n", __FILE__);
	return (0);
}
